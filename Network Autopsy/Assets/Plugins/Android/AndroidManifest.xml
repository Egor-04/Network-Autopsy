<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.unity3d.player"
    xmlns:tools="http://schemas.android.com/tools">

	<!-- ===== НЕОБХОДИМЫЕ РАЗРЕШЕНИЯ ===== -->

	<!-- 1. Обязательные для работы с сетью -->
	<uses-permission android:name="android.permission.INTERNET" />
	<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
	<uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

	<!-- 2. Для диагностики и управления сетью -->
	<uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />

	<!-- 3. Для записи файлов с диагностикой -->
	<uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

	<!-- 4. Для локации (может потребоваться для Wi-Fi сканирования на Android 10+) -->
	<!-- Раскомментируйте при необходимости:
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" 
        android:maxSdkVersion="29" />
    -->

	<!-- ===== НАСТРОЙКИ ПРИЛОЖЕНИЯ ===== -->

	<application
        android:name="com.unity3d.player.UnityPlayerApplication"
        android:icon="@mipmap/app_icon"
        android:label="@string/app_name"
        android:usesCleartextTraffic="true"
        android:allowBackup="true"
        android:supportsRtl="true"
        android:theme="@style/UnityThemeSelector"
        tools:targetApi="31">

		<!-- Основная активность Unity -->
		<activity android:name="com.unity3d.player.UnityPlayerActivity"
                  android:theme="@style/UnityThemeSelector"
                  android:launchMode="singleTask"
                  android:configChanges="mcc|mnc|locale|touchscreen|keyboard|keyboardHidden|navigation|orientation|screenLayout|uiMode|screenSize|smallestScreenSize|fontScale|layoutDirection|density"
                  android:hardwareAccelerated="true"
                  android:excludeFromRecents="false"
                  android:resizeableActivity="true">

			<intent-filter>
				<action android:name="android.intent.action.MAIN" />
				<category android:name="android.intent.category.LAUNCHER" />
			</intent-filter>

			<meta-data android:name="unityplayer.UnityActivity" android:value="true" />
			<meta-data android:name="unityplayer.SkipPermissionsDialog" android:value="true" />
		</activity>

	</application>
</manifest>